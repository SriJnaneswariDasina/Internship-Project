# Web Application Vulnerability Scanner

## Features
- Crawls all pages of a target site
- Tests for:
  - XSS
  - SQL Injection
  - CSRF
- Flask web interface

## Usage
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
